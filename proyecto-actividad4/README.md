# Actividad 4 - Formulario Controlado con Validación Inmediata (React)

## Cómo ejecutar

```bash
npm install
npm run dev
```

Luego abre la URL que indique la terminal (por defecto `http://localhost:5173`).
